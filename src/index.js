import Example from "./scripts/example";

document.addEventListener("DOMContentLoaded", () => {
    // console.log("Hello World!")
    const main = document.getElementById("main");
    new Example(main);  //new Game(main)
});